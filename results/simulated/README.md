# Simulated Results

This directory contains software-reconstructed waveform outputs generated directly from the firmware lookup tables.

These are **not** photographs or live captures from the original hardware. The original prototype has been dismantled and real output images are unavailable.

All outputs are reproducible using the script at `tools/generate_waveforms.py`.
